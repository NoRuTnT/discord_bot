spring:
  config:
    import:
      - optional:file:env/security.env[.properties]
youtube:
  api:
    key: ${YOUTUBE_KEY}

discord:
  token: ${DISCORD_TOKEN}

fastapi:
  url: ${FASTAPI_URL}

gpt:
  api:
    key: ${GPT_KEY}
    texturl: ${GPT_TEXTURL}
    mainurl: ${GPT_MAINURL}
    prompt: ${GPT_PROMPT}
    mainrompt: ${GPT_MAINPROMPT}


server:
  port: ${SERVER_PORT1}