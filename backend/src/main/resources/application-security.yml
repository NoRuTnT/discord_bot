spring:
  config:
    import:
      - optional:file:env/security.env[.properties]
  server:
    port: ${SERVER_PORT}

youtube:
  api:
    key: ${YOUTUBE_KEY}

discord:
  token: ${DISCORD_TOKEN}

gpt:
  api:
    key: ${GPT_KEY}
    url: ${GPT_KEY}

fastapi:
  url: ${FASTAPI_URL}

